import io.ia;
import ia;

class Fac extends Something
{
    int a;
    int[] b;
    public int ComputeFac()
    {
        int num_aux;
        if (num < 1)
        {
           num_aux = 1; 
           i = 0;
           i = 3;
           i = 2;
        } 
        else
        {
            num_aux = num * (this.ComputeFac(num-1));
            i = 3;
            i = 2;
        } 
        return num_aux * this.computeFac(1);
    }

    public int somethingFunction(int num)
    {
        int num;
        return 1;
    }

    public static void main(String[] args)
    {
        int i;
        boolean b;
        int i;
        boolean a;
        int[] a;
        
        //a = !(!this.gt(a, b) && !this.eq(a, b));
        a = i.get() && true;
        afdfdfdfdf = true && !this.gt(a, b); //FIX THIS ONE
        a = !this.gt(a, b);
        i = i + 3;
        a[0] = a.length;
        a = this.method()[3];
        adfdfdfdf = !true && (false && false); //FIX THIS ONE
        a = !new Main();
        a = 1 + 2 * 3 + 4;
        
    }
}
