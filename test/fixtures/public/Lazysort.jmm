import MathUtils; //lowerLimit,upperLimit
import Quicksort;

class Lazysort extends Quicksort {
    public static void main(String[] a) {
        int[] L;
        int i;
		boolean d;
        Lazysort q;

        L = new int[10];

        i = 0;
        while (i < L.length) {
            L[i] = L.length - i;
            i = i + 1;
        }

        q = new Lazysort();

        q.quicksortp(L);
        d = q.printL(L);
    }

    public boolean quicksortp(int[] L) {
        return false;
    }
    
    public boolean beLazy(int[] L) {
        return false;
    }
}
